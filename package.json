{
  "name": "gh-pages",
  "version": "1.0.0",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "deploy": "next build && next export && touch out/.nojekyll && touch out/CNAME && echo jotrinh.dev >> out/CNAME && git add out/ && git commit -m \"Deploy to gh-pages\" && git push --delete origin gh-pages && git subtree push --prefix out origin gh-pages"
  },
  "dependencies": {
    "grommet": "^2.12.0",
    "next": "latest",
    "react": "^16.7.0",
    "react-dom": "^16.7.0",
    "styled-components": "^5.1.0"
  },
  "license": "ISC",
  "devDependencies": {
    "babel-plugin-transform-define": "^1.3.0"
  }
}
